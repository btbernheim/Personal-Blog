<h1 class="title">
	<%= @post.title %>
</h1>
hello
<p class="date">
	Submitted <%= time_ago_in_words(@post.created_at) %> Ago
</p>
<p class="body">
	<%= @post.body %>
</p>

<div>
	<p><%= link_to 'Edit', edit_post_path(@post) %></p>
	<p><%= link_to 'Delete', post_path(@post), method: :delete %></p>
	<p><%= link_to 'Home', root_path(@post) %></p>
</div>